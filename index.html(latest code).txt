<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap">
    <title>HOME</title>

    <style>
        body {
            font-family: 'Roboto', sans-serif;
            margin: 0;
            padding: 0;
            background: url('images/img8.jpeg') fixed;
            background-size: cover;
            color: #fff;
            overflow-x: hidden;
        }

        /* Navbar */
        nav {
            display: flex;
            justify-content: center;
            gap: 20px;
            padding: 20px;
            background: rgba(0, 0, 0, 0.8);
            border-bottom: 2px solid #00fff7;
            box-shadow: 0 5px 15px rgba(0, 255, 255, 0.5);
        }

        nav a {
            color: #00fff7;
            text-decoration: none;
            font-size: 18px;
            padding: 12px 20px;
            border-radius: 30px;
            transition: all 0.4s ease;
        }

        nav a:hover {
            background: linear-gradient(135deg, #00fff7, #ff6ec4, #ffa07a);
            color: #fff;
            transform: scale(1.1);
            box-shadow: 0 0 20px rgba(255, 105, 180, 0.8);
        }

        /* Main Content */
        .container {
            width: 90%;
            margin: 50px auto;
            max-width: 1200px;
            opacity: 0;
            transform: translateY(50px);
            transition: all 1.2s ease-out;
        }

        .container.show {
            opacity: 1;
            transform: translateY(0);
        }

        section {
            padding: 50px;
            margin-bottom: 30px;
            border-radius: 20px;
            background: rgba(0, 0, 0, 0.7);
            box-shadow: 0 10px 25px rgba(0, 255, 255, 0.3);
            transition: all 0.5s ease;
        }

        section:hover {
            transform: translateY(-10px) scale(1.03);
            box-shadow: 0 12px 30px rgba(0, 255, 255, 0.5);
        }

        /* Button Styles */
        .btn {
            display: inline-block;
            padding: 12px 30px;
            background: linear-gradient(135deg, #06f5e1, #03f80f);
            color: #fff;
            border-radius: 30px;
            transition: all 0.4s ease;
        }

        .btn:hover {
            background: linear-gradient(135deg, #ff1361, #fff800);
            transform: translateY(-5px);
        }

        /* Responsive Design */
        @media (max-width: 768px) {
            nav {
                flex-direction: column;
                align-items: center;
            }

            .container {
                width: 95%;
            }

            section {
                padding: 30px;
            }
        }

        /* Carousel Section */
        .carousel-container {
            position: relative;
            max-width: 60%;
            margin: 50px auto;
            overflow: hidden;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.5);
        }

        .carousel-wrapper {
            display: flex;
            transition: transform 0.5s ease-in-out;
        }

        .carousel-slide {
            position: relative;
            min-width: 100%;
            transition: opacity 0.5s ease-in-out;
        }

        .carousel-container img {
            width: 100%;
            height: auto;
        }

        .carousel-btn {
            position: absolute;
            top: 50%;
            transform: translateY(-50%);
            background: rgba(0, 0, 0, 0.5);
            color: white;
            border: none;
            cursor: pointer;
            padding: 10px;
        }

        .prev {
            left: 10px;
        }

        .next {
            right: 10px;
        }

        /* Image Name & Button on Hover */
        .image-info {
            position: absolute;
            bottom: 0;
            width: 100%;
            background: rgba(0, 0, 0, 0.7);
            color: white;
            text-align: center;
            padding: 10px;
            display: none;
        }

        .carousel-slide:hover .image-info {
            display: block;
        }

        .image-btn {
            background: #ff1361;
            color: white;
            padding: 5px 10px;
            margin: 5px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        .image-btn:hover {
            background: #00ff0d;
        }

        /* Footer Loading Animation */
        .loading {
            font-size: 28px;
            text-transform: uppercase;
            text-align: center;
            line-height: 40px;
            position: absolute;
            left: 0;
            right: 0;
            margin: auto;
            bottom: 20px;
            transform: translateY(0%);
            animation: bounce 3s infinite ease-in-out;
        }

        /* Gradient Animation with Glow */
        .loading span {
            background: linear-gradient(45deg, #00f260, #0575e6, #ff0099, #f4c4f3);
            background-size: 400% 400%;
            border-radius: 30px;
            padding: 12px 30px;
            position: relative;
            z-index: 999;
            color: #fff;
            font-weight: bold;
            text-shadow: 2px 2px 20px rgba(0, 255, 255, 0.8), 0 0 10px rgba(255, 0, 255, 0.7);
            animation: gradientMove 5s infinite ease-in-out, glowPulse 2s infinite alternate;
        }

        /* Glowing Background */
        .loading:before {
            content: '';
            background: radial-gradient(circle, rgba(255, 0, 255, 0.2), rgba(0, 255, 255, 0.1), transparent);
            width: 460px;
            height: 70px;
            border-radius: 50px;
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            margin: auto;
            filter: blur(20px);
            animation: pulseGlow 4s infinite ease-in-out;
        }

        /* Vertical Glowing Bar */
        .loading:after {
            content: '';
            width: 16px;
            height: 90px;
            background: linear-gradient(180deg, #00ffcc, #ff0066);
            border-radius: 8px;
            position: absolute;
            top: 50%;
            left: 50%;
            margin-top: -45px;
            margin-left: -8px;
            animation: moveBar 4s infinite ease-in-out;
        }

        /* Keyframe Animations */
        @keyframes gradientMove {
            0% {
                background-position: 0% 50%;
            }

            50% {
                background-position: 100% 50%;
            }

            100% {
                background-position: 0% 50%;
            }
        }

        @keyframes glowPulse {
            0% {
                box-shadow: 0 0 20px rgba(0, 255, 255, 0.8), 0 0 10px rgba(255, 0, 255, 0.7);
            }

            100% {
                box-shadow: 0 0 30px rgba(255, 0, 255, 0.9), 0 0 20px rgba(0, 255, 255, 0.9);
            }
        }

        @keyframes bounce {
            0%, 100% {
                transform: translateY(0%);
            }

            50% {
                transform: translateY(-10px);
            }
        }

        @keyframes moveBar {
            0%, 100% {
                transform: translateY(-50px) rotate(0deg);
            }

            50% {
                transform: translateY(50px) rotate(360deg);
            }
        }

        @keyframes pulseGlow {
            0%, 100% {
                transform: scale(1);
                opacity: 0.8;
            }

            50% {
                transform: scale(1.1);
                opacity: 1;
            }
        }
    </style>
</head>

<body>
    <!-- Navigation Bar -->
    <nav>
        <a href="index.html">🏡 Home</a>
        <a href="pages/explore.html">🔭 Explore</a>
        <a href="pages/services.html">🌀 Services</a>
        <a href="pages/order.html">✍️ Order Now</a>
        <a href="pages/description.html">📜 Description</a>
        <a href="pages/aboutus.html">ℹ️ About Us</a>
    </nav>

    <!-- Main Container -->
    <div class="container show">
        <section>
            <h2>Welcome to Our World 🌍</h2>
            <p>Explore the most immersive, interactive, and fun-filled experiences like never before!</p>
            <a href="#" class="btn">Get Started 🚀</a>
        </section>

        <section>
            <h2>Explore New Horizons 🌌</h2>
            <p>Discover new challenges and exciting features across all our pages.</p>
            <a href="pages/explore.html" class="btn">Explore Now 🔭</a>
        </section>

        <section>
            <h2>Understand the Journey 📚</h2>
            <p>Find detailed information about what we offer and how we make an impact.</p>
            <a href="pages/description.html" class="btn">Read More 📜</a>
        </section>

        <section>
            <h2>Meet the Team 💡</h2>
            <p>Get to know the minds behind this masterpiece.</p>
            <a href="pages/aboutus.html" class="btn">About Us 👨‍💻</a>
        </section>

        <!-- Carousel Section -->
        <div class="carousel-container">
            <div class="carousel-wrapper">
                <div class="carousel-slide" data-image="img1.jpeg">
                    <img src="images/img1.jpeg" alt="Design 1">
                    <div class="image-info">
                        Design 1
                        <button class="image-btn" onclick="orderImage('img1.jpeg')">🛒 Order</button>
                        <button class="image-btn" onclick="addToFavourites('img1.jpeg')">❤️ Add to Fav</button>
                    </div>
                </div>
                <div class="carousel-slide" data-image="img2.jpeg">
                    <img src="images/img2.jpeg" alt="Design 2">
                    <div class="image-info">
                        Design 2
                        <button class="image-btn" onclick="orderImage('img2.jpeg')">🛒 Order</button>
                        <button class="image-btn" onclick="addToFavourites('img2.jpeg')">❤️ Add to Fav</button>
                    </div>
                </div>
                <div class="carousel-slide" data-image="img3.jpeg">
                    <img src="images/img3.jpeg" alt="Design 3">
                    <div class="image-info">
                        Design 3
                        <button class="image-btn" onclick="orderImage('img3.jpeg')">🛒 Order</button>
                        <button class="image-btn" onclick="addToFavourites('img3.jpeg')">❤️ Add to Fav</button>
                    </div>
                </div>
                <div class="carousel-slide" data-image="img4.jpeg">
                    <img src="images/img4.jpeg" alt="Design 4">
                    <div class="image-info">
                        Design 4
                        <button class="image-btn" onclick="orderImage('img4.jpeg')">🛒 Order</button>
                        <button class="image-btn" onclick="addToFavourites('img4.jpeg')">❤️ Add to Fav</button>
                    </div>
                </div>
            </div>
            <button class="carousel-btn prev" onclick="moveSlide(-1)">&#10094;</button>
            <button class="carousel-btn next" onclick="moveSlide(1)">&#10095;</button>
        </div>
    </div>

    <!-- Footer with Loading Section -->
    <footer>
        <div class="loading">
            <span>DESIGNED BY - LAKSHYA & ANKIT</span>
        </div>
    </footer>

    <script>
        let slideIndex = 0;
        const slides = document.querySelectorAll('.carousel-slide');

        function moveSlide(n) {
            slideIndex += n;
            if (slideIndex >= slides.length) slideIndex = 0;
            if (slideIndex < 0) slideIndex = slides.length - 1;

            document.querySelector('.carousel-wrapper').style.transform = `translateX(-${slideIndex * 100}%)`;
        }

        // Auto slide every 5 seconds
        setInterval(() => moveSlide(1), 5000);

        // Swipe support for mobile
        let touchStartX = 0;
        let touchEndX = 0;

        document.querySelector('.carousel-container').addEventListener('touchstart', (e) => {
            touchStartX = e.touches[0].clientX;
        });

        document.querySelector('.carousel-container').addEventListener('touchend', (e) => {
            touchEndX = e.changedTouches[0].clientX;
            handleSwipe();
        });

        function handleSwipe() {
            if (touchStartX - touchEndX > 50) {
                moveSlide(1);
            } else if (touchEndX - touchStartX > 50) {
                moveSlide(-1);
            }
        }

        // Order or add to favorites
        function orderImage(imageName) {
            alert(`Order placed for: ${imageName}`);
        }

        function addToFavourites(imageName) {
            alert(`${imageName} added to favorites! ❤️`);
        }
    </script>
</body>

</html>